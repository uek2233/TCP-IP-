# TCP/IP 网络编程 示例程序索引

> 基于《TCP/IP 网络编程》(尹圣雨 著) 各章节示例代码。
> 更多详细笔记请参考 [云盘链接](https://cloudreve.xxh123.online)。

## 目录

- [第 1 章 网络编程入门](#第-1-章-网络编程入门)
- [第 2 章 套接字类型与协议](#第-2-章-套接字类型与协议)
- [第 3 章 地址族与数据序列](#第-3-章-地址族与数据序列)
- [第 4 章 基于 TCP 的服务器端/客户端](#第-4-章-基于-tcp-的服务器端客户端)
- [第 5 章 基于 TCP 的服务器端/客户端 (2)](#第-5-章-基于-tcp-的服务器端客户端-2)
- [第 6 章 UDP 数据传输](#第-6-章-udp-数据传输)
- [第 7 章 优雅地断开套接字连接](#第-7-章-优雅地断开套接字连接)
- [第 8 章 域名与网络地址](#第-8-章-域名与网络地址)
- [第 9 章 套接字的多种可选项](#第-9-章-套接字的多种可选项)
- [第 10 章 多进程服务器端](#第-10-章-多进程服务器端)
- [第 11 章 进程间通信](#第-11-章-进程间通信)
- [第 12 章 I/O 复用](#第-12-章-io-复用)
- [第 13 章 多种 I/O 函数](#第-13-章-多种-io-函数)
- [第 14 章 多播与广播](#第-14-章-多播与广播)
- [附录 other](#附录-other)

---

## 第 1 章 网络编程入门

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `hello_server.c` | 1.1 | TCP "Hello World" 服务端——向客户端发送 "Hello world" 字符串 | - |
| `hello_client.c` | 1.1 | TCP "Hello World" 客户端——连接服务器并接收打印字符串 | - |

## 第 2 章 套接字类型与协议

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `tcp_server.c` | 2.1 | TCP 服务端——向客户端发送 "Hello world!" 字符串 | - |
| `tcp_client.c` | 2.1 | TCP 客户端——循环读取服务器发送的 "Hello world!" 数据 | - |

## 第 3 章 地址族与数据序列

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `endian_conv.c` | 3.3 | 字节序转换演示——使用 `htons`/`htonl` 展示主机序到网络序的转换 | - |
| `inet_addr.c` | 3.4 | 使用 `inet_addr()` 验证合法与非法点分十进制地址 | - |
| `inet_aton.c` | 3.4 | 使用 `inet_aton()` 将字符串地址转换为网络字节序整数 | - |
| `inet_ntoa.c` | 3.4 | 使用 `inet_ntoa()` 将网络地址转换为点分十进制字符串 | - |

## 第 4 章 基于 TCP 的服务器端/客户端

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `echo_server.c` | 4.1 | TCP 回声服务端——迭代模型，每次处理一个客户端连接并回显消息 | - |
| `echo_client.c` | 4.1 | TCP 回声客户端——向服务器发送消息并接收回声（基础版） | - |

## 第 5 章 基于 TCP 的服务器端/客户端 (2)

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `echo_server.c` | 5.1.1 | TCP 回声服务端——迭代式回声服务器（与 4.1 结构相同） | - |
| `echo_client2.c` | 5.1.1 | TCP 回声客户端增强版——确保完整接收所有回声数据（处理 TCP 边界问题） | - |
| `echo_server.c` | 5.1.2 | TCP 计算器服务端——接收操作数和运算符并返回计算结果 | - |
| `echo_server1.c` | 5.1.2 | TCP 计算器服务端变体——使用动态内存分配的另一种解析方式 | - |
| `echo_client.c` | 5.1.2 | TCP 计算器客户端——发送操作数个数、操作数和运算符 | - |
| `tcp_server.c` | 5.kh-5 | TCP 字符串逐段发送服务端——按固定长度(4字节)分段发送字符串 | - |
| `tcp_client.c` | 5.kh-5 | TCP 字符串逐段接收客户端——按固定长度(4字节)分段接收数据 | - |
| `tcp_server.c` | 5.kh-6 | TCP 文件传输服务端——接收文件名请求，读取文件并发送给客户端 | - |
| `tcp_client.c` | 5.kh-6 | TCP 文件请求客户端——向服务器请求指定文件名，接收并保存文件 | - |

## 第 6 章 UDP 数据传输

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `uecho_server.c` | 6.2 | UDP 回声服务端——使用 `recvfrom()`/`sendto()` 接收并回显客户端消息 | [🔗](https://cloudreve.xxh123.online/s/P1ho) |
| `uecho_client.c` | 6.2 | UDP 回声客户端——通过未连接 UDP 套接字发送消息并接收回声 | [🔗](https://cloudreve.xxh123.online/s/Klsd) |
| `bound_host1.c` | 6.3.1 | 已连接 UDP 发送端——对 UDP 套接字调用 `bind()` 后连续发送三条消息 | - |
| `bound_host2.c` | 6.3.1 | 已连接 UDP 接收端——接收已绑定地址的 UDP 数据并打印 | - |
| `uecho_client.c` | 6.3.2 | 已连接的 UDP 回声客户端——使用 `connect()` 将 UDP 套接字连接到固定对端 | [🔗](https://cloudreve.xxh123.online/s/zdin) |
| `uecho_server.c` | 6.3.2 | 已连接 UDP 回声服务端——为已连接 UDP 客户端提供回声服务 | - |

## 第 7 章 优雅地断开套接字连接

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `file_server.c` | 7.1 | 基于 TCP 半关闭来发送文件——`shutdown(sock, SHUT_WR)` 发送 FIN | - |
| `file_client.c` | 7.1 | 基于 TCP 半关闭来接收文件——收到 EOF 后仍可发送确认消息 | - |

## 第 8 章 域名与网络地址

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `gethostbyname.c` | 8.1 | DNS 正向解析——通过域名使用 `gethostbyname()` 获取 IP 地址 | - |
| `gethostbyaddr.c` | 8.1 | DNS 反向解析——通过 IP 地址使用 `gethostbyaddr()` 获取主机信息 | - |

## 第 9 章 套接字的多种可选项

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `get_buf.c` | 9.1 | 获取套接字缓冲区大小——使用 `getsockopt()` 读取 `SO_SNDBUF` / `SO_RCVBUF` | - |
| `set_buf.c` | 9.1 | 设置套接字缓冲区大小——使用 `setsockopt()` 设置发送和接收缓冲区 | - |
| `sock_type.c` | 9.1 | 获取套接字类型——使用 `getsockopt()` 读取 `SO_TYPE` 区分 TCP 和 UDP | - |
| `reuseadr_eserver.c` | 9.2 | TCP 回声服务端——设置 `SO_REUSEADDR` 选项后提供回声服务 | - |
| `echo_client.c` | 9.2 | TCP 回声客户端——配合 `SO_REUSEADDR` 服务端测试 | - |

## 第 10 章 多进程服务器端

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `fork.c` | 10.1 | 进程创建——通过 `fork()` 创建子进程，观察全局/局部变量独立拷贝 | - |
| `zombie.c` | 10.2 | 僵尸进程演示——父进程不回收子进程导致僵尸进程产生 | - |
| `wait.c` | 10.2 | 销毁子进程——使用 `wait()` 等待子进程结束并获取退出状态 | - |
| `waitpid.c` | 10.2 | 非阻塞销毁子进程——使用 `waitpid()` + `WNOHANG` 轮询子进程状态 | - |
| `signal.c` | 10.3 | 通过信号处理函数来结束子进程——注册 `SIGALRM` 和 `SIGINT` 处理函数 | - |
| `sigaction.c` | 10.3 | 通过 `sigaction()` 来结束子进程——注册 `SIGALRM` 信号处理并循环等待 | - |
| `remove_zombie.c` | 10.3 | 通过信号来消灭僵尸进程——注册 `SIGCHLD` 处理函数，子进程退出时自动回收 | - |
| `echo_mpserv.c` | 10.4 | 通过进程来实现服务器并发——每次 `accept()` 后 `fork()` 子进程处理连接 | - |
| `echo_mpclnt.c` | 10.4 | 多进程客户端——使用 `fork()` 分离输入和输出处理 | - |
| `echo_mpserver.c` | 10.5 | 多进程并发服务器——`fork()` + `SIGCHLD` + `SIGINT` 信号处理 | - |
| `echo_mpclient.c` | 10.5 | 通过多进程来实现客户端的 I/O 分离——父子进程分别处理读写 | - |

## 第 11 章 进程间通信

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `pipe1.c` | 11.1 | 管道通信——父进程通过管道向子进程发送字符串 | - |
| `pipe3.c` | 11.1 | 利用管道实现进程之间双向通信——使用两个管道实现父子进程间双向通信 | [🔗](https://cloudreve.xxh123.online/s/YXIq) |
| `echo_storserv.c` | 11.2 | 利用管道通信实现保存与回声——将客户端发送的数据通过管道传递给子进程写入文件 | [🔗](https://cloudreve.xxh123.online/s/mzS8) |

## 第 12 章 I/O 复用

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `select.c` | 12.1 | 通过 `select()` 函数来实现 I/O 复用——同时监视标准输入和文件描述符 | [🔗](https://cloudreve.xxh123.online/s/wkTZ) |
| `echo_selecterv.c` | 12.1 | 通过 `select` 实现 I/O 复用服务器——单进程同时处理多个客户端连接 | [🔗](https://cloudreve.xxh123.online/s/G7fG) |

## 第 13 章 多种 I/O 函数

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `oob_send.c` | 13.1 | `MSG_OOB` 发送紧急消息——客户端——发送普通数据和紧急数据 | - |
| `oob_recv.c` | 13.1 | `MSG_OOB` 接收紧急消息——服务端——通过 `SIGURG` 信号处理函数接收紧急数据 | - |
| `peek_send.c` | 13.2 | 检查输入缓冲——客户端——发送数据供服务端 peek | - |
| `peek_recv.c` | 13.2 | 检查输入缓冲——服务端——使用 `MSG_PEEK` 偷窥数据而不移除 | - |
| `writev.c` | 13.2 | `writev()` 函数对数据进行整合传输——将分散的多块数据一次发送 | - |
| `readv.c` | 13.2 | `readv()` 函数对数据进行整合接收——将接收的数据分散到多个缓冲区 | - |

## 第 14 章 多播与广播

| 文件 | 章节 | 功能描述 | 笔记 |
|------|------|---------|------|
| `news_sender.c` | 14.1 | 多播的发送者——向组播地址发送文件内容，设置 `IP_MULTICAST_TTL` | - |
| `news_receiver.c` | 14.1 | 多播的接收者——使用 `IP_ADD_MEMBERSHIP` 加入组播组并接收数据 | - |
| `news_sender_brd.c` | 14.2 | 广播的发送者——设置 `SO_BROADCAST` 选项后向广播地址发送文件内容 | - |
| `news_receiver_brd.c` | 14.2 | 广播的接收者——绑定端口后接收 UDP 广播消息 | - |

## 附录 other

| 文件 | 功能描述 | 笔记 |
|------|---------|------|
| `file_server.c` | TCP 文件上传服务端——先发送文件大小(8字节)，再发送文件内容 | - |
| `file_client.c` | TCP 文件下载客户端——连接服务器请求文件名并完整接收文件 | - |

---

> **编译提示：** 本项目使用 CMake 构建系统，根目录执行 `./build.sh` 即可编译所有示例。
> 如果遇到 `storage size of 'xxx' isn't known` 错误，请在源文件顶部添加 `#define _GNU_SOURCE`。
